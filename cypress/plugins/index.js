/**
 * ⚠️ ARQUIVO OBSOLETO - NÃO USADO NO CYPRESS 10+
 * 
 * Este arquivo foi usado em versões antigas do Cypress (< 10).
 * No Cypress 10+, as configurações de plugins são feitas diretamente
 * no arquivo cypress.config.js através da função setupNodeEvents().
 * 
 * Este arquivo está mantido apenas como referência e não é executado.
 * Para adicionar tasks ou plugins, edite cypress.config.js
 */

// module.exports = (on, config) => {
//   // Aqui você pode adicionar tasks personalizadas
//   on('task', {
//     log(message) {
//       console.log(message);
//       return null;
//     },
//     
//     table(message) {
//       console.table(message);
//       return null;
//     }
//   });

//   return config;
// };
